# Qiskit Transpiler MCP Server Environment Configuration
#
# This server doesn't require external credentials for basic transpilation.
# Copy this file to .env and customize as needed.

# Optional: Set default optimization level (0-3, default is 2)
# QISKIT_TRANSPILER_DEFAULT_OPTIMIZATION_LEVEL=2

# Optional: Set logging level (DEBUG, INFO, WARNING, ERROR)
# QISKIT_TRANSPILER_LOG_LEVEL=INFO
