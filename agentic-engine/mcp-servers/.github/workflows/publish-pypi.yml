name: Publish to PyPI

on:
  release:
    types: [published]
  workflow_dispatch:
    inputs:
      package:
        description: 'Package to publish'
        required: true
        default: 'all'
        type: choice
        options:
          - all
          - meta-package
          - qiskit
          - docs
          - runtime
          - transpiler
          - gym

jobs:
  publish-qiskit:
    if: |
      (github.event_name == 'release' && contains(github.ref, 'qiskit-v')) ||
      (github.event_name == 'workflow_dispatch' && (github.event.inputs.package == 'all' || github.event.inputs.package == 'qiskit'))
    runs-on: ubuntu-latest
    permissions:
      id-token: write  # Required for trusted publishing
      contents: read

    steps:
    - uses: actions/checkout@v4

    - name: Install uv
      uses: astral-sh/setup-uv@v3
      with:
        version: "latest"

    - name: Set up Python
      run: uv python install 3.12

    - name: Build package
      working-directory: ./qiskit-mcp-server
      run: |
        uv build --out-dir dist

    - name: Verify build output
      run: |
        ls -la qiskit-mcp-server/dist/
        if [ ! -d "qiskit-mcp-server/dist" ] || [ -z "$(ls -A qiskit-mcp-server/dist)" ]; then
          echo "ERROR: Build did not produce any distribution files"
          exit 1
        fi

    - name: Publish to PyPI
      uses: pypa/gh-action-pypi-publish@release/v1
      with:
        packages-dir: qiskit-mcp-server/dist/
        verbose: true

  publish-runtime:
    if: |
      (github.event_name == 'release' && contains(github.ref, 'runtime')) ||
      (github.event_name == 'workflow_dispatch' && (github.event.inputs.package == 'all' || github.event.inputs.package == 'runtime'))
    runs-on: ubuntu-latest
    permissions:
      id-token: write  # Required for trusted publishing
      contents: read

    steps:
    - uses: actions/checkout@v4

    - name: Install uv
      uses: astral-sh/setup-uv@v3
      with:
        version: "latest"

    - name: Set up Python
      run: uv python install 3.12

    - name: Build package
      working-directory: ./qiskit-ibm-runtime-mcp-server
      run: |
        uv build --out-dir dist

    - name: Verify build output
      run: |
        ls -la qiskit-ibm-runtime-mcp-server/dist/
        if [ ! -d "qiskit-ibm-runtime-mcp-server/dist" ] || [ -z "$(ls -A qiskit-ibm-runtime-mcp-server/dist)" ]; then
          echo "ERROR: Build did not produce any distribution files"
          exit 1
        fi

    - name: Publish to PyPI
      uses: pypa/gh-action-pypi-publish@release/v1
      with:
        packages-dir: qiskit-ibm-runtime-mcp-server/dist/
        verbose: true

  publish-transpiler:
    if: |
      (github.event_name == 'release' && contains(github.ref, 'transpiler')) ||
      (github.event_name == 'workflow_dispatch' && (github.event.inputs.package == 'all' || github.event.inputs.package == 'transpiler'))
    runs-on: ubuntu-latest
    permissions:
      id-token: write  # Required for trusted publishing
      contents: read

    steps:
    - uses: actions/checkout@v4

    - name: Install uv
      uses: astral-sh/setup-uv@v3
      with:
        version: "latest"

    - name: Set up Python
      run: uv python install 3.12

    - name: Build package
      working-directory: ./qiskit-ibm-transpiler-mcp-server
      run: |
        uv build --out-dir dist

    - name: Verify build output
      run: |
        ls -la qiskit-ibm-transpiler-mcp-server/dist/
        if [ ! -d "qiskit-ibm-transpiler-mcp-server/dist" ] || [ -z "$(ls -A qiskit-ibm-transpiler-mcp-server/dist)" ]; then
          echo "ERROR: Build did not produce any distribution files"
          exit 1
        fi

    - name: Publish to PyPI
      uses: pypa/gh-action-pypi-publish@release/v1
      with:
        packages-dir: qiskit-ibm-transpiler-mcp-server/dist/
        verbose: true

  publish-docs:
    if: |
      (github.event_name == 'release' && contains(github.ref, 'docs')) ||
      (github.event_name == 'workflow_dispatch' && (github.event.inputs.package == 'all' || github.event.inputs.package == 'docs'))
    runs-on: ubuntu-latest
    permissions:
      id-token: write  # Required for trusted publishing
      contents: read

    steps:
    - uses: actions/checkout@v4

    - name: Install uv
      uses: astral-sh/setup-uv@v3
      with:
        version: "latest"

    - name: Set up Python
      run: uv python install 3.12

    - name: Build package
      working-directory: ./qiskit-docs-mcp-server
      run: |
        uv build --out-dir dist

    - name: Verify build output
      run: |
        ls -la qiskit-docs-mcp-server/dist/
        if [ ! -d "qiskit-docs-mcp-server/dist" ] || [ -z "$(ls -A qiskit-docs-mcp-server/dist)" ]; then
          echo "ERROR: Build did not produce any distribution files"
          exit 1
        fi

    - name: Publish to PyPI
      uses: pypa/gh-action-pypi-publish@release/v1
      with:
        packages-dir: qiskit-docs-mcp-server/dist/
        verbose: true

  publish-gym:
    if: |
      (github.event_name == 'release' && contains(github.ref, 'gym')) ||
      (github.event_name == 'workflow_dispatch' && (github.event.inputs.package == 'all' || github.event.inputs.package == 'gym'))
    runs-on: ubuntu-latest
    permissions:
      id-token: write  # Required for trusted publishing
      contents: read

    steps:
    - uses: actions/checkout@v4

    - name: Install uv
      uses: astral-sh/setup-uv@v3
      with:
        version: "latest"

    - name: Set up Python
      run: uv python install 3.12

    - name: Build package
      working-directory: ./qiskit-gym-mcp-server
      run: |
        uv build --out-dir dist

    - name: Verify build output
      run: |
        ls -la qiskit-gym-mcp-server/dist/
        if [ ! -d "qiskit-gym-mcp-server/dist" ] || [ -z "$(ls -A qiskit-gym-mcp-server/dist)" ]; then
          echo "ERROR: Build did not produce any distribution files"
          exit 1
        fi

    - name: Publish to PyPI
      uses: pypa/gh-action-pypi-publish@release/v1
      with:
        packages-dir: qiskit-gym-mcp-server/dist/
        verbose: true

  publish-meta-package:
    runs-on: ubuntu-latest
    # Meta-package should be published after individual servers
    needs: [publish-qiskit, publish-docs, publish-runtime, publish-transpiler, publish-gym]
    # Allow meta-package to run even if individual packages were skipped (already published)
    if: |
      always() &&
      (needs.publish-qiskit.result == 'success' || needs.publish-qiskit.result == 'skipped') &&
      (needs.publish-docs.result == 'success' || needs.publish-docs.result == 'skipped') &&
      (needs.publish-runtime.result == 'success' || needs.publish-runtime.result == 'skipped') &&
      (needs.publish-transpiler.result == 'success' || needs.publish-transpiler.result == 'skipped') &&
      (needs.publish-gym.result == 'success' || needs.publish-gym.result == 'skipped') &&
      ((github.event_name == 'release' && contains(github.ref, 'meta')) ||
       (github.event_name == 'workflow_dispatch' && (github.event.inputs.package == 'all' || github.event.inputs.package == 'meta-package')))
    permissions:
      id-token: write  # Required for trusted publishing
      contents: read

    steps:
    - uses: actions/checkout@v4

    - name: Install uv
      uses: astral-sh/setup-uv@v3
      with:
        version: "latest"

    - name: Set up Python
      run: uv python install 3.12

    - name: Build meta-package
      run: |
        uv build

    - name: Publish to PyPI
      uses: pypa/gh-action-pypi-publish@release/v1
      with:
        packages-dir: dist/
        verbose: true
